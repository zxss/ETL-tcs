# Базовый стек: всё, что нужно штатному конвейеру (ETL → валидация → прогноз).
# Устанавливается всегда.
#
#   pip install -r requirements.txt
#
# Тяжёлые ML-библиотеки вынесены отдельно и опциональны:
#   pip install -r requirements-ml.txt              # torch, hmmlearn
# Отключённый контур новостей:
#   pip install -r contrib/experimental_news/requirements.txt

# --- Загрузка данных ---------------------------------------------------------
aiohttp~=3.13           # асинхронные запросы к T-Invest API
psycopg2-binary~=2.9    # PostgreSQL
python-dotenv~=1.2      # чтение .env в config.py

# --- Расчёты (валидация стратегий, признаки, прогноз) ------------------------
numpy~=2.0
pandas~=2.3
scipy~=1.13             # статистики контура валидации

# Тесты идут на stdlib unittest — отдельной зависимости не требуют:
#   python3 -m unittest discover -s tests -t .
# pytest опционален (tests/conftest.py работает с обоими раннерами).
