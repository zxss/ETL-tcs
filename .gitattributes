# Выкладка исследовательского кода идёт через `git archive <ветка> | tar -x -C
# /opt/etl-r4`, поэтому всё, что попадает в архив, ПЕРЕЗАПИСЫВАЕТ файл на
# сервере. Реестр испытаний дописывается прогонами на сервере — при выкладке он
# затирался версией из репозитория (24.09.2026 так потерялись 10 из 18 записей).
# Исключаем из архива именно его; audit/ целиком исключать нельзя — там лежит
# audit/out/cost_matrix.csv, без которой не считаются издержки.
audit/r4_research/trials.jsonl export-ignore
